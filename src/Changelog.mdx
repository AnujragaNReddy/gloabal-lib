import { Meta, Markdown } from '@storybook/blocks';

import changelog from '../CHANGELOG.md?raw';
import pkg from '../package.json';

<Meta title="Docs/Changelog" />

# Changelog

**Current version:** v{pkg.version}

This page renders `CHANGELOG.md` verbatim. It is regenerated by
`npm run version-packages` (Changesets) on every release, so "what changed in
which version" always matches what was published.

<Markdown>{changelog}</Markdown>
